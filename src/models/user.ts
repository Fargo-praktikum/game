export default interface User {
    id: number | null,
    login: string | null,
    displayName: string | null,
    firstName: string | null,
    secondName: string | null,
    email: string | null,
    avatar: string | null,
    phone: string | null,
}
